stages:
  - test

test-Windows:
  stage: test
  tags:
    - Windows
    - Terracloud
  script:
    ant run-tests
  artifacts:
    reports:
      junit: test-reports/raw/TEST-*.xml
    paths:
      - test-reports/*
