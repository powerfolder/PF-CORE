stages:
  - test

variables:
  GIT_DEPTH: "1"

test-Linux:
  stage: test
  resource_group: tests-Linux-$CI_RUNNER_ID
  tags:
    - Linux
  script:
    - pkill -f java || true
    - ant run-tests
  artifacts:
    reports:
      junit: test-reports/raw/TEST-*.xml
    paths:
      - test-reports/*

test-macOS:
  stage: test
  resource_group: tests-macOS-$CI_RUNNER_ID
  tags:
    - macOS
  script:
    - pkill -f java || true
    - ant run-tests
  artifacts:
    reports:
      junit: test-reports/raw/TEST-*.xml
    paths:
      - test-reports/*

test-Windows:
  stage: test
  resource_group: tests-Windows-$CI_RUNNER_ID
  tags:
    - Windows
  script:
    - powershell -Command "Get-Process java -ErrorAction SilentlyContinue | Stop-Process -Force; Write-Host 'Old Java processes killed (if any)'"
    - ant run-tests
  artifacts:
    reports:
      junit: test-reports/raw/TEST-*.xml
    paths:
      - test-reports/*