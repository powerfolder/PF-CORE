<folders>
	#foreach($folder in $folderList)        
	<folder>
		<id>$folder.getId()</id>
		<name>$folder.getName()</name>
		#if ($folder.isSecret())
			<type>Private</type>
		#else 
			<type>Public</type>				
		#end		
	</folder>		
	#end
</folders>
